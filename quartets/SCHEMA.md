# Quartet schema

Five fields. The first four state the case. The fifth is the audit.

```
situation    the moment you are in when you would reach for this
heuristic    what to do
concept      why it works
exceptions   where it stops working
verdict      accepted | rejected | open — dated, with reasoning
```

## The verdict field

A source being admitted does not make its heuristics true. Admission is about whether a text can
fill four fields. **Acceptance is about whether the reasoning survives inspection.**

So a quartet can be fully stated and then refused. That is not a gap in the collection — it is the
most informative entry in it.

### Rejections are the signal

A rejection records what I was, on the date I refused it. Read in sequence, the rejections are a
record of character over time in a way the acceptances are not — anyone can agree with Valluvar.

**Rules:**

- A rejection states the heuristic **at full strength** first. You may not refuse a case you have
  not stated fairly, and the four fields are filled before the verdict is written.
- Every verdict carries a **date**.
- Every rejection carries **what would reopen it** — the evidence or argument that would change the
  answer. A rejection with no reopening condition is a preference, not a judgement.
- A reversal **appends**. It never overwrites. The commit history is the time series, and losing it
  would delete exactly the thing this field exists to record.
